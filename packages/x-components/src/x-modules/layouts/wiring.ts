import { createWiring } from '../../wiring/wiring.utils';

/**
 * Wiring configuration for the {@link LayoutsXModule | layouts module}.
 *
 * @internal
 */
export const layoutsWiring = createWiring({});
